# Updater Batch Job

This directory contains the batch job responsible for updating the `banks.json` containing FD interest rates.

When implemented, the job should run its update script (in whatever language chosen) to overwrite the data file located at `../web/src/lib/banks.json`.
